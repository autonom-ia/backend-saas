{
  "name": "autonomia-api-saas",
  "version": "1.0.0",
  "description": "Serviço SAAS com CRUD de produtos.",
  "main": "index.js",
  "scripts": {
    "deploy": "serverless deploy --stage prod",
    "deploy:prod": "serverless deploy --stage prod",
    "create:domain": "serverless create_domain --stage prod"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.700.0"
  },
  "devDependencies": {
    "serverless": "^3.35.2",
    "serverless-domain-manager": "^8.0.0"
  }
}
