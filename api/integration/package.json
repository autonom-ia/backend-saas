{
  "name": "autonomia-api-integration",
  "version": "1.0.0",
  "description": "Módulo de integrações de API por account",
  "main": "index.js",
  "scripts": {
    "deploy": "serverless deploy --stage prod",
    "deploy:prod": "serverless deploy --stage prod",
    "create:domain": "serverless create_domain --stage prod"
  },
  "dependencies": {
    "ioredis": "^5.6.1"
  },
  "devDependencies": {
    "serverless": "^3.35.2",
    "serverless-domain-manager": "^8.0.0"
  }
}
